# Backend Configuration
NODE_ENV=development
PORT=5000

# Supabase Cloud
SUPABASE_URL=https://omxyeagavmybmyqppudf.supabase.co
SUPABASE_ANON_KEY=sb_publishable_UEbY4BlonnynGiN7j4c_rg_S3C1nv7c
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database (Direct PostgreSQL - Optional)
DATABASE_URL=postgresql://postgres:password@localhost:5432/integrity

# JWT Secret (for custom tokens if needed)
JWT_SECRET=your-jwt-secret-key-here

# ADO credentials encryption key
ADO_CREDENTIALS_KEY=replace-with-a-strong-random-value

# Optional: secure server-side ADO credentials (for agent-based connection)
# If configured, Admin can connect projects without typing PAT in UI.
ADO_AGENT_ORG_URL=https://dev.azure.com/your-org
ADO_AGENT_PROJECT=your-ado-project-name
ADO_AGENT_PAT=your-ado-pat

# CORS
CORS_ORIGIN=http://localhost:5173

# Logging
LOG_LEVEL=info
